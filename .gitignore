.claude/
node_modules/
www/
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
*.apk
# Signing keys must never reach the repo — it is public, and anyone holding the
# key could publish an "update" that installs straight over hers.
*.jks
*.keystore
keystore.base64.txt
keystore.password.txt
__pycache__/
*.pyc
Thumbs.db
.DS_Store
